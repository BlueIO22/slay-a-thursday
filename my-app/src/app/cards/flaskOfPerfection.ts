import Card from "../lib/card";
import State from "../lib/state";

export default {
  name: "Flask of perfection",
  action: (state: State) => {
    // do something ...
    return state;
  },
} as Card;
